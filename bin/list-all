#!/usr/bin/env bash

set -euo pipefail

GIT_REPO="https://github.com/crowdin/crowdin-cli.git"

git ls-remote --tags --refs "$GIT_REPO" \
  | awk '{print $2}' \
  | sed 's#refs/tags/##' \
  | sed 's/^v//' \
  | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' \
  | sort -V
